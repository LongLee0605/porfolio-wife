export {
  CONTENT_UPDATED_AT,
  absoluteUrl,
  siteConfig,
} from "@/lib/seo/site";
export { buildRootMetadata, rootViewport } from "@/lib/seo/metadata";
export {
  buildJsonLdGraph,
  buildPersonJsonLd,
  buildProfilePageJsonLd,
  buildWebsiteJsonLd,
} from "@/lib/seo/jsonld";
